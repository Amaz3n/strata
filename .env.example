# App URL used for OAuth redirects. Production must be HTTPS and match Intuit exactly.
NEXT_PUBLIC_APP_URL=https://app.example.com

# Supabase project connection. Publishable/legacy anon keys are safe for public clients;
# the service-role key is server-only and must never be included in the iOS app.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# QuickBooks Online OAuth credentials.
# Use sandbox credentials with QBO_SANDBOX=true.
# Use production credentials with QBO_SANDBOX=false.
QBO_CLIENT_ID=
QBO_CLIENT_SECRET=
QBO_SANDBOX=false
QBO_WEBHOOK_VERIFIER_TOKEN=

# Stripe webhook signing secrets. Use STRIPE_WEBHOOK_SECRET for platform account events
# and STRIPE_CONNECT_WEBHOOK_SECRET for connected-account events.
STRIPE_WEBHOOK_SECRET=
STRIPE_CONNECT_WEBHOOK_SECRET=
FINTECH_PAYMENTS_EXECUTION_ENABLED=false
FINTECH_PAYMENTS_RECONCILIATION_ENABLED=false
FINTECH_PAYMENTS_MODE=test
# Keep false until provider, legal, risk, operations, and customer enablement sign-off.
FINTECH_PAYMENTS_LIVE_MODE_APPROVED=false

# Server-side token encryption key. Must be 32 bytes raw, 64 hex chars, or base64 for 32 bytes.
TOKEN_ENCRYPTION_KEY=

# Required. Keys every external portal credential: link-token hashing, PIN
# verification cookies, and external account sessions. Generate with
# `openssl rand -hex 32`. There is no fallback — the portal surface fails loudly
# if this is unset, because the old fallback chain silently degraded to
# SUPABASE_SERVICE_ROLE_KEY. Rotating it signs external users out and
# invalidates outstanding PIN verifications.
PORTAL_ACCESS_SECRET=

# Transactional email (Resend). RESEND_FROM_EMAIL's domain must be verified for
# sending; org mail is sent as "<Org Name>" <org-slug@that-domain>.
RESEND_API_KEY=
RESEND_FROM_EMAIL=

# Inbound email. Unset = every inbound feature is dark: emailed vendor bills
# never arrive, and project correspondence cannot claim an address. There is no
# in-app switch for this and it is not per-organization — one receiving domain
# serves the whole installation.
#
#   1. Add a receiving domain in Resend. The managed catch-all (`<id>.resend.app`)
#      needs no DNS and is what PAYABLES_INBOUND_DOMAIN expects.
#   2. Point an `email.received` webhook at
#      https://<NEXT_PUBLIC_APP_URL>/api/webhooks/resend-inbound and copy its
#      signing secret into RESEND_INBOUND_WEBHOOK_SECRET. Without the secret the
#      route answers 503 and Resend retries forever.
#
# Addresses are then derived, never configured: <org-slug>@domain receives
# vendor bills, and project-<project-slug>@domain receives a project's mail once
# someone with correspondence.write enables it on that project.
PAYABLES_INBOUND_DOMAIN=
RESEND_INBOUND_WEBHOOK_SECRET=
# Optional pretty forwarding aliases in your own mail host (ap-<org-slug>@domain).
# Forwarding preserves the original To: header, so these resolve too.
PAYABLES_INBOUND_ALIAS_DOMAIN=arcnaples.com
PAYABLES_INBOUND_ALIAS_PREFIX=ap-

# Cron/API protection for background QBO processors.
CRON_SECRET=

# Codex-backed platform issue review. CODEX_REVIEW_GITHUB_TOKEN needs permission
# to dispatch workflows for CODEX_REVIEW_GITHUB_REPOSITORY, e.g. owner/repo.
# CODEX_REVIEW_CALLBACK_SECRET must match the GitHub secret
# ARC_CODEX_REVIEW_CALLBACK_SECRET used by .github/workflows/codex-bug-review.yml.
CODEX_REVIEW_GITHUB_REPOSITORY=
CODEX_REVIEW_GITHUB_REF=main
CODEX_REVIEW_GITHUB_TOKEN=
CODEX_REVIEW_CALLBACK_SECRET=

# Sentry error tracking. Use the public DSN for browser capture and the private
# DSN for server/worker capture when available.
NEXT_PUBLIC_SENTRY_DSN=
SENTRY_DSN=
SENTRY_ORG=
SENTRY_PROJECT=
SENTRY_AUTH_TOKEN=
SENTRY_ENVIRONMENT=production
SENTRY_TRACES_SAMPLE_RATE=0.1
NEXT_PUBLIC_SENTRY_TRACES_SAMPLE_RATE=0.05
NEXT_PUBLIC_SENTRY_REPLAY_SESSION_SAMPLE_RATE=0
NEXT_PUBLIC_SENTRY_REPLAY_ERROR_SAMPLE_RATE=1

# Apple Push Notification service for the native iOS app.
# The .p8 private key should be stored with escaped newlines (\n) when entered
# as a single environment variable.
APNS_KEY_ID=
APNS_TEAM_ID=
APNS_AUTH_KEY=
APNS_BUNDLE_ID=com.arcprojects.mobile
APNS_ENVIRONMENT=production
# Plaid bank feeds (Arc Books; use sandbox until institution validation is complete)
PLAID_CLIENT_ID=
PLAID_SECRET=
PLAID_ENV=sandbox
# 32-byte key encoded as base64, used to encrypt provider access tokens at rest.
TOKEN_ENCRYPTION_KEY=
