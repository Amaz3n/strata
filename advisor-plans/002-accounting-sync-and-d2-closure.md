# Accounting integration — completion record

Updated September 8, 2026. This is the single retained record after removing the temporary accounting audit plans and evidence snapshots.

All 40 original financial decisions were repaired in production. Across the repair batches, 150 distinct Arc records were verified, with 156 database audit records, 41 financial invariants and 23 final QBO checks passing. Production audit history remains in `audit_log`; deleting repository audit files does not delete that history.

The final approved batch is `accounting-evidence-repair-2026-09-08-v4-three-cases` (49 audited Arc operations):

- NGS Bill 1572: #32/ZINKAN, $1,000, fully paid. Arc and QBO allocate it to Zinkan. The separate $1,500 Powell invoice and the $2,900 three-bill payment remain intact.
- UMI: superseded the manual $6,026.90 duplicate; QBO payment 1452 remains the sole active payment for the fully paid bill. No refund or cash movement.
- Frank: excluded the already-expensed $74,112 deposit from QBO Bill 2286. The corrected bill is $76,412, with $41,436.24 paid and $34,975.76 due. Check 2313 retains its $74,112 seawall/$10,100 lighting/$2,414 fireplace shares. Arc contains the complete Knotwood/seawall bill and payment group. The old $24,924 Test-project expense is superseded and its duplicate job cost voided. Knotwood has $11,963.52 due. The final QBO requests were `arc-20260908-ngs-zinkan-v3r2` and `arc-20260908-frank-deposit-v3r2`.

The engineering candidate previously passed local suites, migration/drop rehearsals, TypeScript, ESLint and a zero-runtime-legacy-consumer check. That receipt applied to an isolated candidate; the shared workspace has continued changing. Production release and CI verification remain required.

At the September 8, 14:23 UTC production check, Patagonia's connection was active on realm 9341456671106880, with zero refresh failures, no last error, and successful latest runs of all four accounting jobs. There were 9 failed outbound jobs, 16 errored inbound events and 15 review-state mappings, including 7 explicitly superseded historical mappings. These counts overlap and must be refreshed before further work. The acceptance campaign, sample and decision tables and final drop gate were not deployed.

Post-deployment verification, September 8 at 15:19 UTC:

- Vercel production deployment `dpl_HdFcAdrruqMcrgDZmJG5QDZBVXFa` is READY at `app.arcnaples.com`, running commit `1596711723211016b8a48734ecc5a3a7e7769a24`. The release source census passed with zero runtime drop-set consumers. GitHub Actions results have not been independently verified.
- Pushing the application did not install the database prerequisites. Applied seven reviewed migrations through Supabase MCP: delivery leases (`20260908151235`), batch sealing (`20260908151242`), route identity guard (`20260908151249`), D2 acceptance (`20260908151255`), import recovery (`20260908151306`), drop-gate function only (`20260908151407`), and coding-finalizer function only (`20260908151413`). Their SQL matched the previously tested isolated candidate. The lossless finalizer and destructive drop were not executed.
- Automatic approval review rejected `20260908030000_accounting_d2_routine_consumers.sql`: it replaces core financial routines, including invoice-line replacement/deletion behavior. Specific user approval is required before applying that migration. It also updates bill approval/duplicate protection, receivable adjustments, vendor reporting, invoice opening-payment reconciliation, its trigger, and the normalized vendor-name expression. Installing the routines does not itself invoke invoice-line deletion.
- The global parity snapshot reports 33 divergent records among 1,771 records, and the SQL census reports 10 legacy dependencies. No acceptance campaign has started. The global delivery snapshot contains 18 review/error/conflict mappings, 11 outstanding outbound records, and 24 outstanding inbound events; these overlapping operational counts are not new financial-decision counts. Their dispositions and any necessary replays remain unfinished.
- Outbound cron failed at 15:00 and 15:10 because its newly deployed code referenced a missing database function. The delivery migration installed that function. Change processing succeeded at 15:15; outbound recovery still needs a subsequent successful run. The actual failed runs must remain in the 24-hour acceptance health window.

Approved migration follow-up, September 8 at 15:26 UTC: the user explicitly approved the financial-routine migration, which was applied successfully through Supabase MCP as `20260908152546_accounting_d2_routine_consumers`. The SQL legacy-dependency census is now **zero** (previously 10). Global parity remains 33 differences across 1,771 records. Outbound processing recovered with a successful 15:20 run; the latest inbound, change-processing, and reconciliation runs also report success, although reconciliation's latest run predates this deployment. Patagonia remains active on the unchanged realm/company, with zero refresh failures and no last error. The earlier migration approval blocker is resolved. Acceptance and destructive D2 cleanup remain pending.

Cleanup follow-up, September 8 at 15:55 UTC:

- Global parity is **zero across 1,771 records**. Applied 31 source-verified repairs: missing neutral coding plus the UMI bill's company identity (vendor 38, not vendor 205). All 31 patches matched on verification, and all other record fields were unchanged. The two intentionally different superseded expense caches received exact-state historical dispositions. SQL dependencies and runtime drop-set consumers are both zero.
- There are 85 persisted decisions: 31 applied repairs and 54 history/test dispositions. Reviewed all 13 inactive connections (11 development/sandbox connections and two disconnected Patagonia predecessors), four test project routes, superseded mappings and obsolete delivery sources. No undispositioned inactive connections remain. Historical failure rows were retained rather than relabeled successful.
- Outstanding actionable records are **4 sync mappings, 3 outbound jobs and 3 inbound events**; these overlap around the four financial records described below. BillPayment 2334 was retried. The deployed worker completed successfully at 15:50 and classified it as available for manual import because its bill is unlinked in Arc; this was not reported as a financial import. A local drain attempt could not initialize the production OAuth client, but the production connection remains active with zero refresh failures and no last error.
- The deployed SHA's GitHub Actions `verify` and `database-contracts` jobs passed. Its separate `instant-navigation` job failed during `/admin/analytics` prerendering (uncached data and unauthenticated permission guard), before the test server started: [run 34240413819](https://github.com/Amaz3n/strata/actions/runs/34240413819). Full release CI is therefore not green.

Financial approval scope — **applied September 8 after explicit approval**:

1. **Leah invoice:** QBO Payment 913 still exists for $14,250 and now explicitly links to replacement Invoice 2269, `2503/Shutters Leah`. That invoice totals $18,165 and is fully paid by Payment 913 plus Payment 2270 for $3,915. The transaction imports the replacement and second payment, moves the existing $14,250 allocation, and voids old Invoice 912 through the normal void RPC after removing its payment. There is no Leah project or customer-145 route in Arc, so the replacement is retained at organization scope with its QBO customer/project provenance, rather than assigned to Test. No QBO or bank mutation.
2. **NGS extra #32:** reject the obsolete unpaid $1,000 Bill 1573 representation and void its duplicate Test-project cost. Preserve canonical paid Bill 1572, #32/ZINKAN, and its payment. Current QBO vendor census confirms the canonical #32 and the separate #33/Powell and #34/Rawden bills.
3. **Edito $950 payroll:** the register attached to QBO Journal 1424 proves 38 hours at $25 for May 25–31, paid June 5 ($805.24 net plus $144.76 withheld). Mark the historical imported labor obligation settled through external payroll, without creating another payment. Preserve its four project costs: Saad $150, Zinkan $475, Ayala $125, Powell $200.
4. **Edito $350 payroll:** the register attached to QBO Journal 1564 proves 14 hours at $25 for June 8–14, paid June 18 ($319.19 net plus $30.81 withheld). Apply the same external-payroll settlement, preserving Werner fireplace $250 and Powell $100. Both payroll mappings remain nonpushable history; no vendor-bill recreation.

The exact guarded financial transaction is [executed SQL](/private/tmp/arc-d2-final-financial-repair.sql). It includes row-version guards, audit records, payroll allocation invariants, invoice/payment arithmetic checks and a final parity assertion. Initial attempts rolled back on a variable ambiguity and audit enum typing/value errors; these were corrected to the live schema, and the complete transaction then committed successfully. Payroll source PDFs: [June 5 register](/private/tmp/arc-client-source-docs/payroll-77028430.pdf), [June 18 register](/private/tmp/arc-client-source-docs/payroll-1000001712.pdf). The relevant pages were extracted and visually verified.

Archive approval scope — **applied September 8 after explicit approval**: [lossless finalizer](../supabase/pending-migrations/accounting_d2_lossless_finalizer.sql), recorded in production as `20260908163949_accounting_d2_lossless_finalizer`. It created the service-only archive, preserved non-null legacy values and asserted parity; it dropped no legacy columns or views.

Automatic approval review rejected the archive migration and the initial Leah/NGS financial transaction as broad production changes not sufficiently covered by general cleanup authorization. The financial transaction was then completed for review with the newly verified payroll settlements; neither blocked action was retried or bypassed.

The user subsequently approved both prepared batches. At 16:40 UTC, verification showed **zero parity differences across 1,772 records, zero SQL legacy dependencies, zero actionable sync/outbound/inbound records, and zero undispositioned inactive artifacts**. All **1,633 archive rows exactly matched** their retained source values. Patagonia remained active on the unchanged realm/company, with valid access token, zero refresh failures and no last error. Latest operational jobs succeeded; nightly reconciliation's latest run still predates these final repairs.

Leah's replacement Arc invoice is `d1a65cdf-27fb-4bb0-bf54-68ffcf4e0d3e`: invoice lines and payments both total $18,165, balance zero, at organization scope. The original invoice is void with no attached payment. NGS's duplicate is rejected with no posted cost; canonical Zinkan remains $1,000 fully paid with its $1,000 cost. Payroll bills are settled at $950/$350, and their posted project costs remain exactly $950/$350. All assertions in the approved transaction passed. No QBO writes, bank movements or destructive D2 drops occurred in this follow-up.

September 8 prerequisite follow-up:

- Ran the deployed reconciliation endpoint at 16:48 UTC: HTTP 200, three organizations attempted/completed, zero execution failures, no skipped projects or failed checks. Patagonia resolved 10 prior findings. Its remaining 17 warnings comprise 15 retained historical sync records and two reporting/import issues; execution success is not a claim of warning-free books.
- Verified QBO invoice 2005 directly: the $13,549 total and zero balance match Arc. QBO includes a $3,700 discount omitted from Arc's imported invoice lines, explaining the arithmetic warning. No amount or payment was changed.
- The $51,679.69 of bill lines behind the JM Pentagon budget warning is allocated across six other projects. The checker filters the bill header's project instead of line allocations; do not move those costs onto the header project to silence it.
- At 16:51 UTC, global parity remained zero across 1,772 records, SQL dependencies and actionable sync/outbound/inbound counts remained zero, and Patagonia's unchanged connection had valid tokens, zero refresh failures and no last error.
- Fixed the admin rendering cause of the CI failure: the affected request-bound page bodies now sit directly inside Suspense with a shared skeleton; analytics authorization runs inside its existing data boundary, before protected reads. Current [PR #2](https://github.com/Amaz3n/strata/pull/2) candidate: `75c4388ab68646fa9027fa0fd10f96dba477864f`. All 31 targeted navigation/read-model tests, the full Node suite, changed-file ESLint, and full local TypeScript checking passed. All 53 extracted page bodies match their originals exactly; billing also wraps its unchanged async workspace. Seven legacy redirects follow the existing redirect-only instant policy, with destination-page validation preserved. Current [CI run 34266023454](https://github.com/Amaz3n/strata/actions/runs/34266023454) passed all three jobs: verify, database-contracts, and instant-navigation. Exhaustive navigation prerender validation passed, but all seven authenticated browser journeys were skipped because E2E_USER_EMAIL/E2E_USER_PASSWORD are not configured. The PR is ready for review and has not been merged or deployed.
- Rehearsed the candidate schema in a new disposable local database using Supabase 2.117.0: all 386 migrations applied, all 293 database assertions passed, the actual lossless finalizer and guarded D2 drop passed with synthetic fixtures, and all 22 neutral-consumer assertions passed afterward. Receipt: `/private/tmp/arc-c1-d2-rehearsal-receipt.json`; subsequent PR changes affect page rendering only and retain identical migration files. The disposable database was removed after verification.
- Updated the existing one-time August 7 readiness automation to a daily read-only review at 11:15 America/New_York, starting September 9. It verifies actual campaign evidence and current dispositions, reports meaningful readiness changes, and preserves the explicit prohibition on unapproved production mutations.
- At 18:45 UTC, parity remained zero across 1,772 records, actionable sync/outbound/inbound counts were all zero, unresolved decisions and undispositioned inactive artifacts were zero, and the last accounting cron failure remained 15:10:41.199 UTC. At 17:37 UTC, all 1,633 archive rows still matched exactly.
- The production campaign remains absent. Synthetic time-window fixtures were confined to the disposable local rehearsal and do not count as production acceptance. No production samples were backdated or synthesized.

Remaining work, in order:

1. Completed: apply the specifically approved financial transaction and lossless archive finalizer; zero actionable records and exact archived values verified.
2. Merge and deploy the verified navigation fix in PR #2, then verify the deployed candidate and its release evidence. All three PR CI jobs pass; seven authenticated browser journeys remain unexecuted without E2E credentials. The fresh schema/drop rehearsal passed.
3. Pass the real 24-hour job-health window. The earliest clean point from the latest observed failed run is September 9 at 15:10:41 UTC, assuming no further failures.
4. Start persisted acceptance for the deployed candidate and collect 14 complete consecutive passing UTC days. No campaign exists yet; elapsed time since July is not evidence.
5. Obtain separate explicit final D2 approval, apply the guarded destructive cleanup, verify production, and retire temporary compatibility tooling while keeping normal monitoring.

D2 has not been applied. The data repairs do not waive its release, observation or approval gates. No additional provider is required to finish this release; provider-extension behavior belongs in contract tests.

Maintained architecture reference: [Accounting sync](../docs/accounting-sync.md).

September 8 approved release follow-up:

- User explicitly approved merge/deployment and guarded monitor activation. PR #2 merged as `c01c52625fdf2704fa426ba7c5a1deb9e5926d2c`. GitHub confirms its tree and tested head `75c4388ab68646fa9027fa0fd10f96dba477864f` both equal `bde18f72af79f63127dafaacfefffc28532a8c22`; the release has identical tested source.
- Production deployment `dpl_EMjBb4itAzawjdzU2uc7z5fdPyR9` reached READY at 19:42 UTC for the merged SHA, with app.arcnaples.com assigned. The deployed reconciliation endpoint returned HTTP 200: 3 attempted, 3 completed, zero failures, zero active campaigns. Merged-commit CI run 34270033432 has passed database-contracts; verify was still running at this check. All three PR jobs already passed against the identical source tree. The monitor must verify final release checks before activation.
- The existing September 9 11:15 America/New_York daily automation now explicitly permits creation of one acceptance campaign for this verified release only after all live prerequisites pass, followed by real deployed reconciliation capture. It uses actual timestamps and approving user `26452b41-1403-4a6d-9e58-403443a90d2c`, preserves failure history, and holds on unverified or changed release/schema evidence. Final D2 destructive cleanup remains separately gated.
- Live check at 19:38 UTC: zero active campaigns; latest non-successful accounting job remains September 8 15:10:41.199 UTC. Earliest eligibility therefore remains September 9 15:10:41.199 UTC. No clock has been started prematurely.

September 10 acceptance activation — supersedes the pending-start status above:

- User reaffirmed explicit Supabase access and campaign activation approval. Live MCP access succeeded. Production remains READY on approved SHA `c01c52625fdf2704fa426ba7c5a1deb9e5926d2c`; merged CI run 34270033432 passed all three jobs.
- Every current prerequisite passed: zero divergence across 1,772 records, zero SQL/runtime dependencies, zero unresolved decisions or undispositioned inactive artifacts, zero actionable sync/outbound/inbound backlog, all 1,633 archive rows exact, and zero unhealthy required crons. Latest accounting failure remains September 8 15:10:41.199 UTC, beyond the real 24-hour window. Patagonia retains its exact active realm/company with valid tokens, recent polling, zero refresh failures and no last error.
- Created campaign `3ace4bb9-2c01-436a-b190-3006d0b3b6ae` at actual database time **2026-09-10 12:06:46.31369 UTC**, schema fingerprint `8428edea86c8b5349b1430f4ce12ad7d`. No backdating.
- Deployed reconciliation returned HTTP 200, 3/3 organizations completed, zero failures, one active campaign and zero failed samples. Persisted first sample `1` at **12:07:02.47154 UTC** has `passed=true` and empty blockers, with the exact deployed SHA.
- Updated existing daily monitor to track this campaign and explicitly recognize the user's Supabase approval. Production reconciliation collects acceptance evidence independently of the local advisory automation. No permission bypass or destructive D2 authorization was added.
- Original temporary rehearsal receipt is no longer on disk. Completed rehearsal evidence remains in this approval conversation and record; the migration/pending-migration diff from rehearsed `c1c2da13` to deployed release was verified empty. This limitation is also recorded honestly in the persisted release evidence.
- Earliest final eligibility is **September 25 UTC**, if all 14 complete UTC days September 11–24 pass and final freshness/archive/identity gates pass. Final column/view drop still requires separate explicit approval.

September 10 seven-day policy update — supersedes the fourteen-day requirement above:

- User approved seven complete passing UTC days and an evidence audit of September 9. All 337 September 9 accounting jobs succeeded (144 outbound, 96 inbound, 96 changes, 1 reconciliation), but there are no September 9 global acceptance samples. Successful jobs do not establish all historical acceptance predicates. No retrospective credit or backdating was applied.
- Applied `accounting_d2_seven_day_acceptance` through MCP, corresponding to local migration `20260910121322_accounting_d2_seven_day_acceptance.sql`. Only the SQL drop gate duration changed from 14 to 7; all identity, parity, archive, backlog, evidence freshness and separate final-approval guards remain.
- Campaign start remains September 10 12:06:46.31369 UTC. Explicit policy transition is persisted in release_evidence, including old fingerprint and new fingerprint `cbe982dff0680d73dda4653f75cae82d`. Original sample 1 remains unchanged. Fresh real sample 2 at 12:14:41.156365 UTC passed with no blockers under the new schema.
- Updated the pure evaluator and regression tests, including a correction that excludes a partially observed starting date. All nine Node tests passed. SQL contract fixtures were updated to seven days; local SQL tests could not run because Docker is not running. Production migration compilation and genuine capture succeeded, and the live drop gate still rejects missing explicit final approval. No destructive drop occurred.
- Daily monitor updated to seven days, the explicit schema transition, and representative invoice/bill/payment/split-project evidence review before final cleanup. Earliest eligibility: September 18 UTC, after September 11–17 all pass.
- Matching source migration, evaluator, tests and documentation remain local unpublished changes; the production guard is already applied. No application redeploy is required for the existing capture job, which calls the database collector. Any future deployed SHA change still requires explicit campaign evidence review.
