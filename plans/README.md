# Implementation Plans

Generated by the improve skill on 2026-07-07, against commit `7e98e5de`
(note: the working tree carried ~74 files of uncommitted changes at audit
time; plan excerpts reflect the working tree, not the bare commit). Execute in
the order below unless dependencies say otherwise. Each executor: read the
plan fully before starting, honor its STOP conditions, and update your row
when done.

Verification commands (created by plan 001, used by all others):
`pnpm lint` · `pnpm typecheck` · `pnpm test`

⚠️ Repo-wide safety rule for all executors: **local `.env.local` points at the
PRODUCTION Supabase database.** No plan in this directory authorizes running
SQL, migrations, seeds, or any data mutation against it. Never run `pnpm dev`
or `pnpm build`.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Verification baseline (test + typecheck scripts) | P1 | S | — | TODO — revised to convert seven `bun:test` TS tests to `node:test` before wiring scripts |
| 002 | Sanitize signature SVG (stored XSS) | P1 | S | 001 | TODO |
| 003 | Outbox atomic claim + stale recovery + FIFO | P1 | M | 001 | TODO |
| 004 | Recurring-invoice idempotency + late-fee error surfacing | P1 | M | 001 | TODO |
| 005 | Estimate actions → ActionResult pattern | P2 | M | 001 | TODO |
| 006 | Cron/portal auth fail-closed (env-var gate — read Step 0 first) | P1 | S | 001 | TODO |
| 007 | Repo hygiene sweep (tarball, lockfile, dead dep/export) | P2 | S | 001 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale).

## Dependency notes

- **001 first, everything else in any order.** All other plans use
  `pnpm typecheck` / `pnpm test` as verification gates, which 001 creates.
- 006 has a **deploy-blocking operational precondition**: `CRON_SECRET` and
  `PORTAL_ACCESS_SECRET` must exist in the production environment before its
  code lands (its Step 0 verifies; merging without them 401s every cron /
  breaks portal PIN cookies).
- 004 and 006 both add invariant-style test files wired into the `test`
  script; if executed concurrently, resolve the trivial `package.json` merge.
- 005 was not in the user's selected batch ("Top 6") but ranked #5 in the
  vetted findings table and was included as a mechanical, low-risk
  repo-rule fix. Mark REJECTED here if unwanted.

## Findings considered and rejected (do not re-audit)

- **"Retainage row never written for new invoices"** (subagent claim): wrong —
  `createInvoice` inserts retainage at `lib/services/invoices.ts:434`. The
  real residue (dead `createInvoiceWithRetainage` export with an ignored
  parameter and zero callers) is handled in plan 007.
- **picomatch high advisory as a standalone security finding**: reachable only
  via Sentry build tooling, not runtime; folded into plan 007 as an override
  pin.
- **Sequential awaits in `app/(app)/projects/[id]/*` pages**: fetches are
  dependent (project first, then project-scoped data) — by design.
- **"Heavy PDF/viewer libs eagerly bundled"**: pdfjs/openseadragon/esign
  surfaces already use dynamic `await import(...)`; not a finding. (Recharts
  eager import in `components/financials/gmp-control-section.tsx` IS real but
  low-impact; unplanned, below.)
- **Local dev pointing at production Supabase**: documented, deliberate
  tradeoff (CLAUDE.md) — not re-reported, but every plan carries the warning.

## Audited, confirmed, but unplanned (candidates for a future run)

Ordered roughly by leverage; ask for a plan when ready to act:

1. **Money-service characterization tests** — draws, budgets, change-orders,
   commitments, retainage, payables have zero behavioral coverage; 40 of the
   existing "regression" assertions are source-text greps. Prerequisite for
   any refactor of those services.
2. **Invoice/CO totals single source of truth** — replicate the proven
   `lib/financials/estimate-totals.ts` pattern for invoices/change-orders;
   depends on item 1.
3. **QBO float money serialization** (`lib/services/qbo-sync.ts:309-314` etc.)
   — `quantity * cents / 100` in binary float; needs a QBO-sandbox repro
   (investigate-plan, not fix-plan).
4. **Outbox enqueue dedupe constraint** — check-then-act in
   `lib/services/outbox.ts:26-56`; durable fix is a partial unique index
   (production migration, operator approval). Companion to plan 003.
5. **ActionResult sweep of remaining action files** (e.g.
   `app/(app)/pipeline/actions.ts`) — same recipe as plan 005.
6. **Per-row UPDATE loops** in change-order recalc
   (`lib/services/change-orders.ts:1505-1515, 1914-1927`).
7. **Money-formatting consolidation** — ~20 reimplementations with divergent
   cents policy; canonical helper is `lib/utils.ts:12`.
8. **ESLint 9 / eslint-config-next 16 migration** — the only static gate runs
   on an EOL linter.
9. **`-new` filename renames** — `bid-package-detail-client-new.tsx`,
   `bid-portal-client-new.tsx` + delete dead `bid-portal-client.tsx`.
10. **Portal `cost_code_id` ownership validation**
    (`lib/services/cost-plus.ts:1965-1991`).
11. **Portal login enumeration hardening**
    (`lib/services/external-portal-auth.ts:440-493`).
12. Lower tier: recharts dynamic import, uncapped file/daily-log queries +
    virtualization, god-file splits (needs item 1), gradual `as any` triage
    (703 sites), docs/ gameplan status index, admin-desk revenue metrics
    (direction; `lib/services/admin.ts:52-57` TODOs).
